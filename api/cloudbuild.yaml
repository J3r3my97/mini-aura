# Cloud Build configuration for API service
# Triggered on push to main branch (api/ directory changes)

# Use dedicated CI/CD service account
serviceAccount: 'projects/mini-aura/serviceAccounts/mini-me-cicd@mini-aura.iam.gserviceaccount.com'

steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/mini-me/api:$SHORT_SHA'
      - '-t'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/mini-me/api:latest'
      - '.'
    dir: 'api'

  # Push the container image to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/mini-me/api:$SHORT_SHA'

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/mini-me/api:latest'

  # Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'mini-me-api'
      - '--image=us-central1-docker.pkg.dev/$PROJECT_ID/mini-me/api:$SHORT_SHA'
      - '--region=us-central1'
      - '--platform=managed'
      - '--allow-unauthenticated'
      - '--memory=512Mi'
      - '--cpu=1'
      - '--min-instances=1'
      - '--max-instances=10'
      - '--concurrency=80'
      - '--timeout=60s'
      - '--set-env-vars=PROJECT_ID=$PROJECT_ID,FIREBASE_PROJECT_ID=$PROJECT_ID,FRONTEND_URL=https://mini-aura.vercel.app'
      - '--set-secrets=CLAUDE_API_KEY=CLAUDE_API_KEY:latest,STRIPE_SECRET_KEY=STRIPE_SECRET_KEY:latest,STRIPE_WEBHOOK_SECRET=STRIPE_WEBHOOK_SECRET:latest,STRIPE_1_CREDIT_PRICE_ID=STRIPE_1_CREDIT_PRICE_ID:latest,STRIPE_5_CREDIT_PRICE_ID=STRIPE_5_CREDIT_PRICE_ID:latest,STRIPE_10_CREDIT_PRICE_ID=STRIPE_10_CREDIT_PRICE_ID:latest,GOOGLE_APPLICATION_CREDENTIALS_JSON=mini-me-storage-key:latest'
      - '--service-account=mini-me-api@$PROJECT_ID.iam.gserviceaccount.com'

# Store images in Artifact Registry
images:
  - 'us-central1-docker.pkg.dev/$PROJECT_ID/mini-me/api:$SHORT_SHA'
  - 'us-central1-docker.pkg.dev/$PROJECT_ID/mini-me/api:latest'

# Timeout for entire build
timeout: 1200s

options:
  # Use faster machine type for builds
  machineType: 'N1_HIGHCPU_8'
  logging: CLOUD_LOGGING_ONLY
